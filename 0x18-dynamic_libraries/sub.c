#include <stdlib.h>
#include <stdio.h>

/**
 * sub - subtracts two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the difference
 */

int sub(int a, int b)
{
	return (a - b);
}

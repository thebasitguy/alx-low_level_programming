#include <stdlib.h>
#include <stdio.h>

/**
 * mod - modulo of two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the remainder
 */

int mod(int a, int b)
{
	return (a % b);
}
